---
layout: docs
title: Deploying on Google Cloud (GCE)
description: Moved — see Quick Start or Manual Installation.
---

This page has moved. Deploying bioAF to Google Cloud now lives in two places:

- **[Quick Start]({{ '/docs/' | relative_url }})** — the scripted path (recommended for most users)
- **[Manual Installation > Setup & Deploy]({{ '/docs/installation/setup/' | relative_url }})** — the manual path (Windows, custom IAM, or just want to click through things yourself)

Both converge once you're SSH'd into the VM. The full web UI walkthrough lives on the [Quick Start page]({{ '/docs/#set-up-bioaf-in-the-web-ui' | relative_url }}).
